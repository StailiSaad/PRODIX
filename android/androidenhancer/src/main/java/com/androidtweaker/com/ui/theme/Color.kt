package com.androidtweaker.com.ui.theme

import androidx.compose.ui.graphics.Color

// ── Dark theme ──────────────────────────────────────────
val BackgroundDark = Color(0xFF000000)
val SurfaceDark = Color(0xFF0D0D0D)
val SurfaceContainerDark = Color(0xFF1A1A1A)
val SurfaceContainerHighDark = Color(0xFF242424)
val SurfaceContainerHighestDark = Color(0xFF2E2E2E)

// ── Light theme ─────────────────────────────────────────
val BackgroundLight = Color(0xFFFFFFFF)
val SurfaceLight = Color(0xFFF5F5F5)
val SurfaceContainerLight = Color(0xFFEEEEEE)
val SurfaceContainerHighLight = Color(0xFFE0E0E0)
val SurfaceContainerHighestLight = Color(0xFFD6D6D6)

// ── Shared palette ──────────────────────────────────────
val Primary = Color(0xFF00E5FF)           // Neon blue — primary action
val PrimaryDim = Color(0xFF00B8CC)        // Dimmed primary
val Secondary = Color(0xFF2979FF)         // Blue accent
val Accent = Color(0xFF7C4DFF)            // Violet accent
val Success = Color(0xFF00E676)           // Green
val NeonGray = Color(0xFFB0BEC5)          // Neon gray

// ── Dark on-colors ──────────────────────────────────────
val OnBackgroundDark = Color(0xFFE8EAED)
val OnSurfaceDark = Color(0xFFE8EAED)
val OnSurfaceVariantDark = Color(0xFF9AA0A6)
val OnPrimary = Color(0xFF001F29)

// ── Light on-colors ─────────────────────────────────────
val OnBackgroundLight = Color(0xFF000000)
val OnSurfaceLight = Color(0xFF000000)
val OnSurfaceVariantLight = Color(0xFF5F6368)

val Error = Color(0xFFFF5252)
val OutlineDark = Color(0xFF3C4043)
val OutlineVariantDark = Color(0xFF2D3135)
val OutlineLight = Color(0xFFDADCE0)
val OutlineVariantLight = Color(0xFFC4C7CC)
val SurfaceTint = Color(0xFF00E5FF)

val GlassOverlay = Color(0x1AFFFFFF)
